import React from 'react';
import {_Header} from './assets/scss/Header.scss';

function Header(props) {
    return (
        <h1 className={_Header}>{'SASS & SCSS II'}</h1>
    );
}

export default Header;